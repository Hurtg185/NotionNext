import React from 'react';
import TranslatorUI from '../components/TranslatorUI'; // 从 components 文件夹导入 UI 组件

// 这是一个页面 (Page)
export default function TranslatorPage() {
  // 这个页面非常干净，只做一件事：显示翻译器的 UI
  return <TranslatorUI />;
    }
